<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".JabberwockyActivity" >

    <ScrollView
       android:id="@+id/webViewScrool"
        android:layout_width="wrap_content"
        android:layout_height="480dp" >

        <WebView
            android:id="@+id/webView1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/default_margin" />

    </ScrollView>

    <Button
        android:id="@+id/btn_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btn_wiki"
        android:layout_alignBottom="@+id/btn_wiki"
        android:layout_margin="@dimen/default_margin"
        android:layout_toRightOf="@+id/btn_wiki"
        android:onClick="changeImage"
        android:text="@string/image" />

    <Button
        android:id="@+id/btn_wiki"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/webViewScrool"
        android:layout_margin="@dimen/default_margin"
        android:layout_marginTop="14dp"
        android:onClick="toWiki"
        android:text="@string/wiki" />

</RelativeLayout>